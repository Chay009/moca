/**
 * TextSimple Component Types
 * Extends base TxtNodeProperties with TextSimple-specific props
 */

import { TxtNodeProperties } from '../textProperties';

/**
 * TextSimple component props
 * Inherits all 90+ Motion Canvas Txt properties
 * No additional effect-specific props
 */
export interface TextSimpleProps extends TxtNodeProperties {
  // No additional props - uses all base Txt properties
}
