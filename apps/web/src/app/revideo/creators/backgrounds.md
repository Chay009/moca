Motion Canvas recommends THREE options:
Project-level (simplest) - Set background in makeProject settings - applies to ALL scenes globally
Scene-level - Use view.fill() to set background for that specific scene
Node-level - Add a Rect as first child, add all content after it