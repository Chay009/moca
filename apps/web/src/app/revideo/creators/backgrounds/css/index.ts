/**
 * CSS-based backgrounds (Solid + Gradient)
 * Handles solid colors and CSS gradients
 * in future we are adding a new component called 
 */

export { parseBackgroundString } from './backgroundParser';
