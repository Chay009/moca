/**
 * Image backgrounds
 * Handles image URLs as backgrounds
 */

// Currently no separate logic - images are handled in backgroundComposition.tsx
// Future: Add image loading, caching, transformations here
export {};
